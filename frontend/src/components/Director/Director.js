import './director.css';

function Director({ item }) {
    return (
      // <a href="#" className="tag">{item}</a>
      <span href="#" className="director">{item}</span>

    );
}

export default Director;